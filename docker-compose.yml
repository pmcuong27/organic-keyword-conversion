services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: blend
      POSTGRES_PASSWORD: blend
      POSTGRES_DB: gsc_ga4_blend
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
