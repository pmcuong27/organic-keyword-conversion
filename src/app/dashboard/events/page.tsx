export default function EventsPage() {
  return (
    <div className="p-6">
      <h1 className="text-lg font-semibold">Conversion Events</h1>
      <p className="mt-1 text-sm text-muted-foreground">
        Event-level organic conversion explorer — open a keyword row sheet for per-event breakdown today.
      </p>
    </div>
  );
}
